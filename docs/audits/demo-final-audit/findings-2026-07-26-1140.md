# SWI Admin — QA de volume (rodada Fable) — 2026-07-26 11:40

> Reexecução completa do QA de usabilidade/coerência de dados da branch
> `fix/admin-volume-qa`, pós-commit `93743d9` + paliativo do Combobox
> (working tree). Stack: api :3001 (Docker, `--build` de 93743d9), Vite :5173,
> db :5433. Login: `admin@swi.local`.
>
> Nota de protocolo: o playbook referenciado pelo skill
> (`docs/audits/demo-final-audit/playbook.md`) não existe no repo. Matriz de
> cobertura usada: as 11 telas do QA de volume das rodadas anteriores.
> Severidades: **Blocker** (impede demo/uso real), **Polish** (visível mas não
> impede), **Nit** (detalhe).

## Status das correções (2026-07-26 12:20)

| Achado | Status | Onde |
| ------ | ------ | ---- |
| POLISH-1 abas não filtram | ✅ CORRIGIDO | `MonitoringLayout.tsx` + `tier` no DTO (`api/monitoring.ts`, `mockApi/monitoring.ts`) |
| POLISH-2 `VITE_MAPBOX_TOKEN` ausente | ✅ CORRIGIDO | token público adicionado ao `swi-admin/.env.local` (gitignored) |
| POLISH-3 `<button>` aninhado no DS | ✅ CORRIGIDO | bump DS **0.1.118 → 0.1.119** (`ActivitiesOverviewCard`) |
| NIT-1 "(1) administradores" | ✅ CORRIGIDO | novo `lib/pluralize.ts` + `AdminsList`/`EmployeesList` |
| NIT-2 card default-expandido morto | ✅ CORRIGIDO | `MonitoringLayout.tsx` (por tier, não por id mock) |
| NIT-3 "Ver Todos" decorativo | ✅ CORRIGIDO | derruba o filtro de tier (padrão do Dashboard) |
| NIT-4 kcal idêntico pra todos | ✅ CORRIGIDO | `simulatedCaloriesFor` + `seedId` nos 2 callers |

Verificação: admin **623/623** testes (+8 novos), `tsc --noEmit` limpo nos dois
projetos, DS 24/24. No app real: abas 2/5/2, "Ver Todos" 5→9, `/tasks` com
**0 erros de console**, "(1) administrador cadastrado", e 3 curvas de kcal
distintas (Worker Demo `34,51,59…` · Adriana `37,55,63…` · Admin `43,56,58…`).

Rota de socorro com o token real: `GET api.mapbox.com/directions/v5/mapbox/
driving-traffic/…` → **200**, rótulos `11 s / 103 m / 27 s` (eram `— / 25 m / —`),
polilinha seguindo a via e atribuição "© Mapbox © OpenStreetMap".
Evidência: `qa3-rescue-route-mapbox.png`.

**Nenhum achado em aberto.**

## Sumário

**11 telas auditadas, 0 Blockers, 3 Polish, 4 Nits. Veredicto: APRESENTÁVEL.**
Toda a espinha dorsal de coerência de dados da rodada `93743d9` segue de pé:
9 funcionários em todos os lugares (KPIs 2+5+2, mapa, listas, diretório do
chat), avatares reais carregados em 100% das superfícies, tiers espelhados
entre dashboard e filtros de alertas (2/5/2), 12 relatórios com responsáveis e
fotos reais, 5 tarefas nas abas certas, chat persistindo no Postgres, settings
com o paliativo do Combobox funcionando ao vivo. Os 3 Polish: (1) as abas de
`/monitoring/*` se anunciam "Filtro de status" com badge "2" mas listam os 9
em todas as rotas; (2) a rota de socorro SEMPRE cai no fallback de linha reta
com rótulos "—" porque `VITE_MAPBOX_TOKEN` não está no `.env.local` — para a
demo, é 1 linha de env; (3) `<button>` aninhado do DS `ActivitiesOverviewCard`
loga erro de console em `/tarefas` (bump de DS já rastreado). Nits: plural
"(1) administradores", card default-expandido morto no monitoring, "Ver Todos"
decorativo, série de kcal idêntica para todas as pessoas. Nenhum achado
impede demo ou uso real; os dois primeiros Polish são os únicos visíveis para
um cliente atento.

## Pré-flight

- `GET /health` → `{"status":"ok"}` ✅
- Seed: 9 workers + 1 admin na empresa demo (CNPJ 00000000000191), 0 perfis sem
  avatar, 12 reports, 5 work orders ✅

---

## Achados

### 1. Dashboard (Home) — ✅ sem achados novos

- KPIs 2 Excelentes + 5 Desgastados + 2 Urgentes = 9 = tile "9 Funcionários" ✅
- Tiles: 1 Administradores / 9 Funcionários / 3 Novos relatórios / 12 Câmeras ✅
- MapBanner: 9 marcadores, todos com avatar presigned distinto (`worker-*.png`),
  todos dentro do canvas neste instante; tiers no `aria-label`: 2 good / 5 alert /
  2 low — espelham os KPIs ✅
- Badge "Dados simulados" na régua de Alertas de Desgaste ✅
- Alertas de fadiga: Carlos Santos 127 bpm 15/10, Worker Demo 139 bpm 16/10 —
  coerentes com os 2 urgentes ✅
- Previsão do tempo com cursor AGORA ✅ · Console: 0 erros
- **Conhecido/aberto (não é achado novo):** enquadramento do MapBanner segue
  one-shot (`didFitRef`) — pinos podem sair da moldura com o simulador; decisão
  entre 3 opções segue pendente com o usuário.
- Evidência: `screenshots/2026-07-26/qa3-dashboard.png` · Repro: login → `/`

### 2. Mapa geral (`/maps/general`) — ✅ sem achados novos

- Modo Operador: 9 marcadores, cada um com avatar `<img>` carregado
  (`naturalWidth > 0`) e nome no `aria-label` — zero círculos cinza ✅
- Modos aditivos: Operador + Câmeras = 21 marcadores (9 avatares + 12 câmeras
  "Câmera Norte 1…") — **12 = tile "Câmeras ativas" do dashboard** ✅
- Mapa de calor ativa sem erro de console ✅
- Estado inicial abre sem nenhum modo ligado (só o basemap Esri) — deliberado,
  os 3 toggles ficam visíveis no canto; não classificado como achado.
- Evidência: `qa3-maps-operador.png`, `qa3-maps-cameras.png` ·
  Repro: `/` → "Ver mapa geral" → clicar "Operador" → clicar "Câmeras"

### 3. Funcionários (lista + detalhe) — ✅ sem achados novos

- Header "(9) funcionários cadastrados" = seed ✅ · 9 cards com idade, tipo
  sanguíneo, cargo e setor; todos os avatares `naturalWidth > 0` ✅
- Worker Demo: fadiga **80,0%** rotulada "Fadiga atual" (escala 0-100 ✅),
  Gênero Masculino, alergia "Poeira de sílica", "Nenhum exame registrado.",
  badge "Dados simulados", minimapa com 1 marcador (posição ao vivo própria) ✅
- Adriana Santos Almeida: Gênero **Feminino**, B-, "Nenhuma alergia
  registrada.", fadiga 34,0% — caminho feminino e estados vazios ✅
- Evidência: `qa3-employees-list.png`, `qa3-employee-detail.png` ·
  Repro: sidebar Funcionários → card Worker Demo / card Adriana

### 4. Administradores (lista + detalhe)

- Card do Admin exibe "Administrador / Gestão" (rótulos livres preservados) ✅
- Detalhe: Masculino, 40 anos, O+, estados vazios declarados, fadiga 9,0%,
  minimapa honesto "Sem posição ao vivo" (admin não emite heartbeat) ✅

**NIT-1 — Pluralização com contagem 1 no header de Administradores**
- Severidade: Nit
- Header lê "Você tem **(1) administradores cadastrados**" — plural para
  contagem 1 (Funcionários com 9 está correto; o template não singulariza).
- Evidência: `qa3-admin-detail.png` (header visível na lista antes do clique;
  texto capturado via innerText: "Você tem (1) administradores cadastrados")
- Repro: 1. login → 2. sidebar "Administradores" → 3. ler o header da lista.
- Elemento: header da lista `/admins` (mesmo componente do header de
  `/employees`).

### 5. Monitoramento (`/monitoring/*`)

- KPIs: 1/9/3/12 + "2 Alertas de fadiga" + 14/9 pressão média + **80.145**
  movimentos (milhar formatado ✅) · Badge "Dados simulados" ✅
- Lista: 9 cards com avatar, idade, sangue, cargo; ordenação põe os 2 do tier
  de fadiga (Carlos Santos, Worker Demo) no topo ✅
- Expansão do card: "Frequência cardíaca crítica 130 bpm — limite recomendado:
  100 bpm" + "Fadiga acumulada — 100 min até fadiga total — desgaste em 93%" +
  3 CTAs ✅

**POLISH-1 — Abas "Filtro de status" não filtram a lista (9 linhas nas 3 abas)**
- Severidade: Polish
- As abas Excelentes / Desgastados / Alertas de Fadiga navegam entre
  `/monitoring/good-conditions`, `/monitoring/desgastados` e
  `/monitoring/alerts`, mas a lista é compartilhada pelo layout e só é filtrada
  pela busca textual — as 3 rotas listam os mesmos 9 funcionários. A aba ativa
  "Alertas de Fadiga" exibe badge vermelho "2" sobre uma lista de 9: o número e
  a lista se contradizem na mesma tela. No dashboard, a mesma régua filtra
  corretamente (aba fadiga → 2 entradas), o que torna a divergência visível
  para o cliente que acabou de sair do Home.
- Evidência: `qa3-monitoring.png` (aba fadiga ativa + badge 2 + 9 linhas);
  medição por aba: `good-conditions: 9, desgastados: 9, alerts: 9 linhas`.
- Repro: 1. login → 2. sidebar "Monitoramento" → 3. clicar cada aba da régua
  "Alertas de Desgaste" → 4. contar os cards (sempre 9).
- Elemento: `Tabs accessibilityLabel="Filtro de status"`
  (`MonitoringLayout.tsx:511-524`); lista em `filteredUsers`
  (`MonitoringLayout.tsx:416-418` — filtra só por `search`).

**NIT-2 — Card default-expandido de `/monitoring/alerts` morreu com ids reais**
- Severidade: Nit
- `initialExpanded` compara com `'emp-04'` (id do roster mock; comentário cita
  Figma 69:14774 querendo o 1º crítico já aberto), mas os ids reais são UUIDs —
  nenhum card abre expandido ao aterrissar na rota.
- Evidência: `qa3-monitoring.png` (nenhum card expandido no load);
  `MonitoringLayout.tsx:391-395`.
- Repro: 1. navegar direto a `/monitoring/alerts` → 2. observar que nenhum
  card está expandido.
- Elemento: `AlertUserCard` / `initialExpanded` (`MonitoringLayout.tsx:394`).

**NIT-3 — "Ver Todos" do monitoramento é toast decorativo**
- Severidade: Nit
- O botão "Ver Todos" (`accessibilityLabel="Ver todos os alertas"`) dispara só
  um toast "Lista completa de alertas" — a lista completa já está na tela.
- Evidência: `MonitoringLayout.tsx:554-559`. Repro: 1. `/monitoring` →
  2. clicar "Ver Todos" → 3. toast sem navegação.

### 6. Alertas + rota de socorro (`/alerts`, `/alerts/:id/rescue/:rid`)

- Filtros do mapa filtram DE VERDADE: Urgência médica → 2 marcadores, Risco →
  5, Sem incidentes → 2, Todos → 9 — espelho exato dos KPIs ✅
- Badge "Dados simulados" ✅ · Clique no pino urgente abre painel Worker Demo
  (117 Bpm / 140/110) com "Criar rota de socorro" ✅
- Candidatos a socorrista: 8 (9 − vítima ✅), ordenados por distância real
  0.02→0.58 km, com sangue/idade; "Melhor opção de ajuda" no mais próximo ✅
- Despacho: modal → Continuar → `?dispatched=true`, pino do socorrista troca
  pro estado "em deslocamento" ✅ · Distância da rota 25 m, coerente com os
  0.02 km da lista ✅

**POLISH-2 — Rota de socorro sempre cai no fallback: `VITE_MAPBOX_TOKEN` ausente**
- Severidade: Polish (ambiente, não código — mas aparece na demo)
- Nenhuma chamada à Mapbox Directions API sai (0 requests em
  `api.mapbox.com`): `fetchRoute` devolve `null` imediatamente porque
  `import.meta.env.VITE_MAPBOX_TOKEN` não existe — `.env.local` do swi-admin
  não define o token (só o `.env.example` tem placeholder). Resultado: toast
  "Rota indisponível", linha RETA entre os pinos e rótulos "— / 25 m / —"
  (tempo e ETA viram travessão; distância é estimativa geométrica).
- Evidência: `qa3-rescue-route.png` (rótulo "25 m" com traços);
  `browser_network_requests` filtrado por `mapbox|directions` → vazio;
  `mapboxDirections.ts:45-46` (bail sem token);
  `AlertsRescueRoute.tsx:162-171` (fallback "—").
- Repro: 1. `/alerts` → 2. chip "Urgência médica" → 3. clicar pino →
  4. "Criar rota de socorro" → 5. "Selecionar" no primeiro candidato →
  6. observar rótulos com "—".
- Correção sugerida (fora do escopo do auditor): adicionar
  `VITE_MAPBOX_TOKEN=pk.…` real ao `swi-admin/.env.local` e recarregar o Vite.

### 7. Relatórios (lista + detalhe) — ✅ sem achados novos

- Lista: **12 cards + contador "12 de 12"** = total do banco ✅ · 39 imagens de
  avatar na tela, 0 quebradas ✅
- Filtro "Filtrar por status" → Concluído: 5 cards, contador "5 de 5", só
  badges CONCLUÍDO ✅
- Detalhe (Relatório de Segurança da Planta Sul): autor Josué Oliveira
  (worker-3), responsáveis "Carlos Santos, Antonio Carlos Figueira" com as
  fotos EXATAS deles (worker-4/worker-5), nenhuma pílula "+N" ✅
- Evidência: `qa3-report-detail.png` · Repro: sidebar Relatórios → card
  "Relatório de Segurança da Planta Sul"; filtro status → Concluído

### 8. Chat — ✅ sem achados novos

- Conversa Jennifer Gomes: painel de identidade real (Feminino / 33 anos / A- /
  Látex), "Tempo até a fadiga total 3h47" + badge "Dados simulados",
  mini-mapa com avatar dela ✅
- Envio real: mensagem "QA Fable 2026-07-26 — teste de envio" renderizou na
  thread E persistiu no Postgres (`Message.body`, 14:54 UTC = 11:54 local
  exibido ✅)
- "Novo Chat" abre diretório com TODOS os 9 (Adriana, Antonio, Ezequiel,
  Josué, Romulo, Worker Demo…) ✅
- Evidência: `qa3-chat.png` · Repro: sidebar chat → conversa Jennifer →
  digitar + Enviar → "Novo Chat"

### 9. Tarefas (lista + form)

- Abas batem 1:1 com o banco: Em Andamento = 1 (Reparo hidráulico), Concluídas
  = 1 (Alocação de maquinário — Frente 12), A Fazer = 3 (Inspeção Técnica,
  Trocar extintores, Vistoria de EPIs) — total 5 = 5 WorkOrders ✅
- `/tasks/new`: form completo (responsáveis com "Nenhum responsável
  atribuído.", setor, datas, checklist, anexos JPG/PNG, Cancelar/Salvar) ✅

**POLISH-3 — `<button>` aninhado no ActivitiesOverviewCard (erro de console, item DS conhecido)**
- Severidade: Polish (aguarda bump do DS — CLAUDE.md proíbe remendo local)
- `/tasks` loga `validateDOMNesting: <button> cannot appear as a descendant of
  <button>` vindo de `ActivitiesOverviewCard` (DS) via `TasksList.tsx:40`. É o
  bump pendente já registrado na sessão anterior (Card externo com
  `accessibilityRole="button"` + `LocationButton` interno — clique borbulha e
  dispara 2 ações). Primeira vez com evidência de console error no QA.
- Evidência: console do Playwright em `/tasks` (1 error, stack aponta
  `swi-design-system/src/components/ActivitiesOverviewCard/ActivitiesOverviewCard.tsx:38`).
- Repro: 1. sidebar "Tarefas" → 2. abrir devtools console → 3. erro presente
  no load.
- Elemento: DS `ActivitiesOverviewCard` (bump v-next; não corrigível no app).

### 10. Configurações + Perfil do usuário — ✅ sem achados novos

- `/user/settings`: prefill completo — Admin, 09/06/1986, CPF/telefone
  mascarados, **Profissão "Administrador" / Setor "Gestão"** (paliativo do
  Combobox vivo no app real), Função, Gerente, O+, Masculino ✅
- `/user/profile`: "Sem posição ao vivo" (admin sem heartbeat), "Nenhum exame
  registrado.", "Nenhuma alergia registrada.", Condições excelentes + badge
  "Dados simulados" ✅
- Evidência: `qa3-user-profile.png` · Repro: avatar do header → perfil;
  sidebar Configurações

**NIT-4 — Série de gasto calórico idêntica para todas as pessoas**
- Severidade: Nit (coberto pelo badge "Dados simulados", mas é o padrão
  "mesmo número em todo lugar" que esta linha de QA caça)
- A curva de kcal (41/57/62/38/55/49/22/19 nos horários 07:15–19:30) é
  byte-a-byte a mesma no detalhe do Worker Demo, do admin e do perfil próprio —
  diferente de fadiga/esforço/vitais, que variam por pessoa via hash do id.
- Evidência: `qa3-employee-detail.png` vs `qa3-user-profile.png` (mesmos
  valores e horários).
- Repro: 1. `/employees/:id` de qualquer pessoa → 2. anotar série de kcal →
  3. `/user/profile` → 4. comparar.

