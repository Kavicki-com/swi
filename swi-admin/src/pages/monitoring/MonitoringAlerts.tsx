// src/pages/monitoring/MonitoringAlerts.tsx
// Child view for /monitoring/alerts (Figma 69:14731). The actual layout
// (KPIs, title, tabs, search, user list, default-expanded card) lives in
// MonitoringLayout — this view contributes nothing extra above the title,
// so it renders nothing and lets the parent handle everything.
export function MonitoringAlerts() {
  return null
}
