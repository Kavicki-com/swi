// Stub for react-native-svg's transform.js (PEG.js-generated CJS) on web.
// extractTransform.js does `import { parse } from './transform'`. The original
// transform.js uses module.exports without ES named exports, which Vite's dev
// server (native browser ESM) cannot interop. SWI Design System components
// don't use SVG transforms, so a no-op parse is safe.
export const parse = (): null => null
export default { parse: () => null }
