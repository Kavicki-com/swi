legacy-peer-deps=true
# Faz o npm recusar a instalação fora do runtime declarado em package.json
# (engines). Sem isto o pin de Node/npm é só documentação: a instalação segue
# em qualquer versão e a divergência só aparece muito depois, em runtime.
engine-strict=true
